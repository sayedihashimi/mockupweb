@page
@model UploadMockupModel

<p>
    Mockupname below determines the folder where these files will be stored. It's also in the URL so make sure you have it set correctly.
    You should include your .bmpr file and all exported .png files.
    To update these files later just re-upload modified files and make sure to use the same value for `mockupname` here.
    <br/><br/>
    Two notes on how to author your balsamiq mockup to get this to work.
    <ul>
        <li>Make sure that all linked elements are directly on the mockup (i.e. not inside of a Symbol)</li>
        <li>Each page in the .bmpr file needs to start at the Top-Left. In each page do a `CTRL+A` and drag to the Top Left as far as possible.</li>
    </ul>
    <br/>
</p>
<form method="post" enctype="multipart/form-data">
    <label for="mockupName">Project name:</label>
    <input name="mockupName" type="text" value="mockupname" />
    <div class="form-group">
        <div class="col-md-10">
            <p>Upload one or more files using this form:</p>
            <input type="file" name="files" multiple />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Upload" />
        </div>
    </div>
</form>

@section scripts{
<environment include="Development">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
</environment>
<environment exclude="Development">
    <link rel="stylesheet" href="https://ajax.aspnetcdn.com/ajax/bootstrap/3.3.7/css/bootstrap.min.css"
          asp-fallback-href="~/lib/bootstrap/dist/css/bootstrap.min.css"
          asp-fallback-test-class="sr-only" asp-fallback-test-property="position" asp-fallback-test-value="absolute" />
</environment>

    <link rel="stylesheet" href="~/css/uploadmockup.css" />
}
